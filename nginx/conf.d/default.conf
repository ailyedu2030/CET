# 英语四级学习系统 - 默认虚拟主机配置
# 此文件为占位符，主要配置在nginx.conf中

# 如果需要额外的虚拟主机配置，可以在此目录下创建新的.conf文件
# 例如：api.conf, admin.conf等

# 示例：API专用配置
# server {
#     listen 80;
#     server_name api.cet4-learning.com;
#     
#     location / {
#         proxy_pass http://cet4_app/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
